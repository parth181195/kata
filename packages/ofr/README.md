# ofr

Dart implementation of the Open Fuji Recipe (OFR) v1 interchange format: `OfrRecipe` (JSON in/out with unknown-key preservation), `OfrValidator` (enums, ranges, half-steps, omission rules) and `OfrHasher` (canonical SHA-256; supports the README scheme and the PR #1 settings-only scheme).

MIT.
