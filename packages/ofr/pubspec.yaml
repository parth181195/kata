name: ofr
description: "Open Fuji Recipe (OFR) v1: model, validator and canonical SHA-256 hasher for Fujifilm film-simulation recipes."
version: 0.1.0
repository: https://github.com/parthjansari/kata
topics: [fujifilm, recipes, ofr]

environment:
  sdk: ^3.11.0

dependencies:
  crypto: ^3.0.6

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
